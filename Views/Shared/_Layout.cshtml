<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - BTL_QuanLyLopHocTrucTuyen</title>
    <script type="importmap"></script>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/BTL_QuanLyLopHocTrucTuyen.styles.css" asp-append-version="true" />
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container">
                <a class="d-flex link-body-emphasis text-decoration-none me-auto" asp-area="" asp-controller="Home" asp-action="Index">
                    <img src="~/favicon.ico" alt="logo" width="24" height="24">
                    <div class="text-secondary ms-2 lh-1">
                        <p class="h5 fw-bolder mb-0 font-family-Roboto">Lo` Mo` Se’</p>
                        <p class="text-muted fs-6 font-family-Roboto mb-0">Developed by WNC.G06</p>
                    </div>
                </a>

                @if (User.Identity!.IsAuthenticated)
                {
                    <div class="col-md-3 text-end">
                        <span class="me-2">Xin chào, @User.Identity!.Name</span>
                        <form onsubmit="logout(event)" class="d-inline">
                            <button type="submit" class="btn btn-outline-danger">Đăng xuất</button>
                        </form>
                    </div>
                }
                else
                {
                    <div class="col-md-3 text-end">
                        <a class="btn btn-outline-primary me-2" asp-area="" asp-controller="Home" asp-action="Login">Đăng nhập</a>
                        <a class="btn btn-primary" asp-area="" asp-controller="Home" asp-action="Register">Dùng thử miễn phí</a>
                    </div>
                }


            </div>
        </nav>
    </header>

    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class=" bg-dark text-white mt-5 pt-5 pb-5">
        <div class="container">
            <div class="row gy-4">
                <!-- Logo + giới thiệu -->
                <div class="col-12 col-md-4">
                    <a asp-controller="Home" asp-action="Index" class="d-flex align-items-center mb-3 text-decoration-none text-white">
                        <img src="~/favicon.ico" alt="Logo" width="40" height="40" class="me-2" />
                        <div>
                            <div class="fw-bold fs-5">Lo’ Mo’ Se’</div>
                            <small class="text-white-50">Developed by WNC.G06</small>
                        </div>
                    </a>
                    <p class="small text-white-50 mb-0">
                        Hệ thống quản lý lớp học trực tuyến giúp giảng viên và sinh viên kết nối, học tập và chia sẻ tài liệu mọi lúc mọi nơi.
                    </p>
                </div>

                <!-- Liên kết nhanh -->
                <div class="col-6 col-md-4">
                    <h6 class="text-uppercase fw-bold text-white mb-3">Liên kết</h6>
                    <ul class="list-unstyled small">
                        <li class="mb-2"><a class="text-white-50 text-decoration-none" href="#">Trang chủ</a></li>
                        <li class="mb-2"><a class="text-white-50 text-decoration-none" href="#about">Giới thiệu</a></li>
                        <li class="mb-2"><a class="text-white-50 text-decoration-none" href="#pricing">Gói dịch vụ</a></li>
                        <li><a class="text-white-50 text-decoration-none" href="#contact">Liên hệ</a></li>
                    </ul>
                </div>

                <!-- Liên hệ -->
                <div class="col-6 col-md-4">
                    <h6 class="text-uppercase fw-bold text-white mb-3">Liên hệ</h6>
                    <ul class="list-unstyled small text-white-50 mb-0">
                        <li class="mb-1"><a href="mailto:info@example.com" class="text-white-50 text-decoration-none">info@example.com</a></li>
                        <li><a href="tel:+84000000000" class="text-white-50 text-decoration-none">+84 0 000 000 000</a></li>
                    </ul>
                </div>
            </div>

            <hr class="border-white-25 my-4" />

            <div class="d-flex flex-column flex-md-row justify-content-between align-items-center small text-white-50">
                <div>© 2025 Lo` Mo` Se’. All rights reserved.</div>
                <div>Developed by <span class="text-white">WNC.G06</span></div>
            </div>
        </div>
    </footer>

    @await Html.PartialAsync("_ModalMessage")

    <script> 
        const logout = (event) => {
            event.preventDefault();
            fetch('api/authen/logout', { method: 'POST' })
                .then(response => {
                    if (response.ok) {
                        window.location.href = '/';
                    } else {
                        alert('Logout failed. Please try again.');
                    }
                })
                .catch(error => {
                    console.error('Error during logout:', error);
                    alert('An error occurred. Please try again.');
                });
        };

         const showModal = (message, redirectUrl = null) => {
            $('#modalBody').text(message);
            const modalEl = $('#modalMessage');
            const modal = new bootstrap.Modal(modalEl);

            modalEl.on('hidden.bs.modal', () => {
                if (redirectUrl) {
                    window.location.href = redirectUrl;
                }
            });

            modal.show();
        };
    </script>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
